name: GitHub Actions Demo # 名称
on: [push] # 触发事件
jobs: # 作业们
  Explore-GitHub-Actions: # 具体作业
    runs-on: ubuntu-latest # 运行机器
    steps: # 步骤
      # 运行
      # 这里对应一个数组，并且数组里面是对象 [{run: echo "🎉 The job was automatically triggered by a ${{ github.event_name }} event."},...]
      - run: echo "🎉 The job was automatically triggered by a ${{ github.event_name }} event."
      - run: echo "🐧 This job is now running on a ${{ runner.os }} server hosted by GitHub!"
      - run: echo "🔎 The name of your branch is ${{ github.ref }} and your repository is ${{ github.repository }}."
      # 这里对应数组中的一个元素 {name: "Check out repository code", uses: "actions/checkout@v2"}
      - name: Check out repository code
        uses: actions/checkout@v2
      - run: echo "💡 The ${{ github.repository }} repository has been cloned to the runner."
      - run: echo "🖥️ The workflow is now ready to test your code on the runner."
      - name: List files in the repository
        run: |
          ls ${{ github.workspace }}
      - run: echo "🍏 This job's status is ${{ job.status }}."
